{
  "name": "@subspace/core",
  "version": "0.0.1",
  "description": "Auto SDK to generate auto id and wallet",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "clean:all": "rm -rf dist && rm -rf node_modules",
    "dev": "yarn build --watch",
    "build": "tsup --config tsup.config.ts",
    "build:test": "tsc",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\""
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@subspace/tsconfig": "*",
    "@types/jest": "^29.5.10",
    "@typescript-eslint/parser": "^6.13.1",
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "prettier": "^3.1.0",
    "ts-jest": "^29.1.1",
    "typescript": "5.3.3",
    "tsup": "8.0.1",
    "util": "0.12.5"
  },
  "dependencies": {
    "@polkadot/api": "^10.11.1",
    "@semaphore-protocol/identity": "^3.15.1",
    "ethers": "6.9.0",
    "@subspace/shamir-secret-sharing": "*"
    
  }
}