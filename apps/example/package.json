{
  "name": "@subpace/example",
  "version": "0.0.1",
  "scripts": {
    "build": "tsup src/index.ts --dts",
    "dev": "yarn build && nodemon dist/index.js",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "serve": "node dist/index.js"
  },
  "dependencies": {
    "@subspace/core": "*",
    "@subspace/shamir-secret-sharing": "*"
  },
  "devDependencies": {
    "@subspace/tsconfig": "*",
    "@types/node": "^20.10.4",
    "nodemon": "3.0.2",
    "ts-node": "^10.9.2",
    "tslib": "^2.6.2",
    "tsup": "8.0.1",
    "typescript": "^5.3.3"
  }
}
